<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:initialKeyguardLayout="@layout/widget_collection"
    android:initialLayout="@layout/widget_collection"
    android:minHeight="@dimen/widget_min_height"
    android:minResizeHeight="@dimen/widget_resize_min_height"
    android:minResizeWidth="@dimen/widget_resize_min_width"
    android:minWidth="@dimen/widget_resize_min_width"
    android:previewImage="@drawable/widget_placeholder"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen|keyguard"
    tools:ignore="UnusedAttribute"></appwidget-provider>